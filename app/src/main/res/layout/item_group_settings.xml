<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/itemGroupSettings"
        android:gravity="center_vertical"
        android:layout_marginStart="@dimen/size_image_item_song"
        android:textColor="@color/hightLight"
        android:textSize="@dimen/text_size_name_song"
        android:layout_width="match_parent"
        android:layout_height="@dimen/size_image_item_song" />

</RelativeLayout>